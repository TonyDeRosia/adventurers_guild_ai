# Smart MUD

Smart MUD is a next generation MUD engine.

It preserves the proven mechanics, commands, displays, and builder workflow of traditional MUDs while replacing static scripting with persistent AI driven world simulation.

The engine is deterministic.

AI enhances the world but never replaces engine authority.

Every world is an installable package.

The same engine can run fantasy, science fiction, modern, horror, or entirely custom worlds.
