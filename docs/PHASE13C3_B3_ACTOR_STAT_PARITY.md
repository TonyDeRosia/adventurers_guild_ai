# Phase 13C3-B3 Actor Stat Parity Audit

Primary reference inspected: `https://github.com/TonyDeRosia/tbamud_adventurers_lair` through raw GitHub source after direct `git clone` was attempted from the container and blocked by an HTTP 403 CONNECT tunnel. The raw source was reachable and inspected for the requested files, with emphasis on `src/fight.c`, `src/act.informative.c`, `src/class.c`, `src/class.h`, `src/race.c`, `src/race.h`, `src/limits.c`, `src/handler.c`, `src/spells.c`, `src/criticalhits.c`, `src/criticalhits.h`, `src/structs.h`, `src/utils.h`, `src/constants.c`, `src/mobact.c`, and `src/db.c`.

| Feature | Reference file/function | Player-facing meaning | Player usage | NPC/mob usage | Smart MUD equivalent | Implementation status | Intentional differences |
|---|---|---|---|---|---|---|---|
| Primary stats shown to players | `act.informative.c` score/stat display, `utils.h` GET_STR/DEX/CON/INT/WIS/CHA macros | Classic six attributes influence hit, armor/evasion, saves, resources, carrying, spells, and class/race behavior. | Player score/stat inspection reads character ability scores. | NPCs use the same stat macros over mob records. | `PrimaryStatValue` over semantic roles physical_power, agility, endurance, intellect, willpower, presence. | Implemented as structured actor-generic snapshot values. | Smart MUD stores semantic roles so worlds can rename attributes without engine changes. |
| Hit-roll behavior | `fight.c` `compute_offensive_hit_value`, `compute_hit_chance_from_values`, `hit` | Hit chance is built from level, hitroll, strength to-hit, mental stats, level gap, and concealment, clamped to a chance range. | Player attacks consume offensive hit and weapon/unarmed attack type. | Mobs use the same function path with their level/stat/hitroll data. | `accuracy` + `hit_bonus` versus `evasion` in `attack_hit_resolution`. | Active and consumed by canonical combat. | Smart MUD separates accuracy rating and hit bonus instead of one Diku hitroll value. |
| Damage-roll behavior | `fight.c` `damage`, weapon attack text, damage severity helpers | Damage messages scale by damage severity; weapon/natural attack types provide verbs. | Player weapons and unarmed attacks produce typed damage messages. | Mob attacks use attack type/natural descriptions. | `attack_power`, `damage_bonus`, weapon profile, natural weapon profile. | Active and consumed by combat. | Smart MUD exposes structured weapon/natural profiles. |
| Armor / armor class | `fight.c` `legacy_ac_to_armor`, `compute_armor`, `compute_armor_class`, `compute_evasion` | AL converts older lower-is-better AC into higher-is-better armor and computes evasion separately. | Player armor, CON/STR, and DEX affect defense. | Mobs use the same computed defense helpers. | `armor` mitigation and `evasion` hit defense. | Active and consumed. | Smart MUD uses explicit armor mitigation formula and separate evasion rating. |
| Saves | `spells.c`, `class.c`, `utils.h` save macros | Saves reduce or avoid spell/effect outcomes. | Player save values participate in spell resolution. | NPC/mob save values participate in spell resolution. | `physical_save`, `mental_save`, `magic_save`. | Active in canonical save resolution. | Smart MUD groups saves by broad effect semantics instead of legacy categories. |
| Critical chance/damage | `criticalhits.c`, `criticalhits.h`, `fight.c` critical calls | AL has customized critical hit tables/procs and severity messaging. | Player attacks can critically hit through critical helpers. | NPC attacks can critically hit through same combat path. | `critical_melee`, `critical_spell`, `critical_heal`, `critical_avoidance`, canonical `critical_damage`. | Implemented; `critical_damage` is a multiplier. | Smart MUD uses a single actor stat multiplier unless an authored attack explicitly overrides it. |
| Player versus NPC stat handling | `fight.c`, `handler.c`, `db.c` mob loading and char macros | Combat functions operate on `char_data` regardless of player/NPC. | Player records provide persistent stats/equipment. | Mob prototypes/instances provide level, hitroll, damroll, armor, attacks. | `ActorStatInput` with player/template/instance/runtime adapters. | Implemented. | Smart MUD does not write NPC stats into player character attribute rows. |
| Natural mob attacks | `fight.c` `attack_hit_text`, mob attack type fields in `structs.h`/`db.c` | Mobs can bite, claw, sting, punch, etc. without player weapons. | Players use fists/unarmed when no weapon. | Mobs use authored attack type/damage. | `NaturalWeaponProfile`. | Implemented for template/body/combat/instance data projection. | Smart MUD natural weapons are structured and shown in inspection. |
| Mob level/combat scaling | `db.c` mob prototype loading, `fight.c` level components | Level participates in hit and reward/scaling calculations. | Players carry level from progression. | Mobs carry level from prototype/instance. | Deterministic legacy template projection. | Implemented. | Smart MUD derives missing primary bases deterministically from level and combat role. |
| Carrying/encumbrance | `handler.c`, `utils.h` carry macros | Strength limits carrying. | Player inventory weight is constrained. | NPCs may carry/equip but normal player UX emphasizes players. | `carry_capacity`, `current_carry_weight`, `encumbrance_percent`, `encumbrance_state`. | Active and displayed. | Smart MUD exposes structured units and state. |
| Initiative/attack timing | `fight.c` `perform_violence` combat-list tick processing | Combat rounds iterate a combat list; AL does not expose a player initiative stat in the inspected combat loop. | Players act during combat pulses. | NPCs act during combat pulses. | Speed stats in snapshot. | Marked inactive. | Smart MUD does not claim initiative/speeds are active until a timing system consumes them. |
| Casting timing | `spells.c` spell execution | Spells resolve through command/spell functions; no separate inspected casting-speed stat authority. | Player casters use spell command rules. | NPC casters use mob/spell AI paths. | `casting_speed`. | Inactive. | Hidden from normal SCORE while unsupported. |
| Movement timing/cost | `act.movement` family and movement helpers referenced by commands | Movement consumes movement/position rules in legacy MUD. | Player movement uses room/terrain checks. | NPC movement uses mobact. | `movement_speed`. | Inactive. | Smart MUD has no current shared speed consumer. |
| Resistances | `spells.c`, damage type handling, affect flags | Damage/effect resistance is type/effect dependent. | Players can receive resist effects/equipment. | NPCs can have resist profiles/effects. | `ResistanceStatValue` loaded from published resistance definitions. | Implemented. | Units are metadata-driven, not assumed for every resistance. |
| Stat inspection commands | `act.informative.c` score/diagnose/consider style commands | Players see their score and condition; builders can inspect more. | Players see allowed self stats. | Exact mob stats are builder/admin inspection data. | `attributes`, `combatstats`, `combatstats breakdown`. | Implemented for canonical snapshots; hidden NPC exact stats are admin/builder scope. | Smart MUD breakdowns include components and formula traces. |

## Gameplay consumer matrix

Active secondary stats have canonical consumers: accuracy/hit_bonus/evasion feed hit resolution; attack_power/damage_bonus feed physical damage; spell_power feeds spell damage; healing_power feeds healing; armor feeds mitigation; critical ratings/avoidance/damage feed critical resolution; saves feed save resolution; resistances feed typed mitigation; resource maxima feed explicit maxima refresh/clamping; carrying values feed encumbrance.

## Speed and presence decisions

Initiative, attack_speed, casting_speed, recovery_speed, and movement_speed are currently calculated for detailed diagnostics but marked inactive with the reason: `Combat rounds do not currently use initiative ordering or speed-based timing.` Presence/charisma remains an active primary attribute; current combat consumers are empty and the snapshot records `combat_consumed=false`.

## Manual Windows acceptance status

Not performed in this Linux container. Suggested commands after launching a Builder/admin test session: `score`, `combatstats`, `combatstats offense`, `combatstats defense`, `combatstats saves`, `combatstats resistances`, `combatstats damage`, `combatstats speed`, `combatstats breakdown accuracy`, `attributes <npc>`, `combatstats <npc>`, `combatstats <npc> damage`, and `combatstats <npc> breakdown attack_power`.
