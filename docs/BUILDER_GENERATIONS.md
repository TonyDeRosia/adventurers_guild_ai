# Builder documentation

See `docs/BUILDER_ARCHITECTURE.md` for the consolidated Builder architecture, session state, editor workflow, natural weapon schema, locks, permissions, history, validation, preview, test room, publish pipeline, generations, rollback, and schema migration details. This stub intentionally avoids duplicating the same material across many files.
