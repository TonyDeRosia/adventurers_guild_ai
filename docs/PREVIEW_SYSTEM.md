# PREVIEW SYSTEM

Preview renders draft content as players would experience it: LOOK text for rooms and mobiles, EXAMINE text for objects, exits, spawns, loot summaries, and combat/natural attack messages. Preview never publishes and never mutates live runtime objects.
