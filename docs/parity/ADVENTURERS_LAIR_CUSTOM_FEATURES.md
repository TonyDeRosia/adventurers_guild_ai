# Adventurer's Lair Custom Features — Phase 16A.1 Verification

Phase 16A.1 separates verified Smart MUD evidence from inferred or source-blocked Adventurer's Lair evidence. The customized reference repository could not be cloned from this environment (`CONNECT tunnel failed, response 403`), so source ownership below names the files that must be checked when access is available.

| Feature | Adventurer's Lair source ownership to verify | Player-visible behavior | Smart MUD equivalent evidence | Migration priority |
| --- | --- | --- | --- | --- |
| AI actor brains | Custom `ai_actor*` files if present, plus `src/mobact.c`, `src/fight.c`, `src/spec_procs.c`. | Mobile aggression, assistance, pursuit, scavenging, and scripted reactions. | `engine/combat_behavior.py`, `engine/living_world.py`, `engine/perception.py`, `worlds/shattered_realms/aggression_profiles/aggression_profiles.json`, `worlds/shattered_realms/controller_profiles/controller_profiles.json`. | High after door/container foundations; do not start before persistent state is stable. |
| Critical hits | Combat damage and spell/heal resolution in `src/fight.c`, `src/magic.c`, and custom critical code if present. | Burst damage/healing messaging and rating-driven outcome changes. | `engine/combat_runtime.py`, `engine/combat.py`, `worlds/shattered_realms/critical_profiles/critical_profiles.json`, combat parity docs. | Medium; Smart MUD already has evidence, verify exact AL formulas before further tuning. |
| Class tracks | `src/class.c`, player skill/class tables, guildmaster special procedures. | Class progression, practice eligibility, level/rank benefits. | `engine/progression.py`, `engine/training.py`, `worlds/shattered_realms/class_tracks/class_tracks.json`, `worlds/shattered_realms/classes/classes.json`. | Medium; strengthen when practice/skill phase begins. |
| Accounts | Login/account code in `src/interpreter.c`, `src/db.c`, plrfile/account storage if customized. | Multi-character login, account metadata, permissions. | Smart MUD has player/session/runtime persistence paths but no verified AL account parity in this audit. | Low for runtime parity unless login/account migration becomes a product goal. |
| ASCII maps | Informative/display code such as `src/act.informative.c` and custom map modules/world files. | Map command, room minimaps, area navigation displays. | `engine/mud_rendering.py`, `engine/display_services.py`, room/world JSON; exact ASCII-map parity unverified. | Low-to-medium; after movement/room state is canonical. |
| DG scripts | `src/dg_*.c`, trigger storage, attach points in world files. | Room/mobile/object triggers, quest scripts, command reactions. | EventBus-style hooks through services, `engine/quests.py`, `engine/dialogue_service.py`; no safe DG-compatible runtime. | High later, but only after stateful runtime foundations and safe trigger design. |
