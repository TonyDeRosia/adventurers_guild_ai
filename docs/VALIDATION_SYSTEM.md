# VALIDATION SYSTEM

Validation is invoked through BuilderService and delegates broad world checks to BuilderWorkspace. Focused validation filters issues for the active object and explains that fixes must be made in the relevant editor before validating again. Editors expose Validate as a first-class menu action.
